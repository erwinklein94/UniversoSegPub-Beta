/* Módulo de estado inicial compartilhado.
   Os dados estáticos simples foram separados em arquivos próprios, mantendo os mesmos nomes globais. */

/* ============================================================ */
/* === ESTADO E HELPERS ======================================= */
/* ============================================================ */
let currTabela = CARGOS_PM;
let currInst = 'pmesp';
let headerModoInicialPortal = true;
